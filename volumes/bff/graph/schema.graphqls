# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type User {
  id: ID!
  name: String!
}

type Product {
  id: ID!
  name: String!
}

type Query {
  getUser(id: Int!): User!
  getUsers(id: Int, name: String): [User!]!
  getProduct(id: Int!): Product!
  getProducts(id: Int, name: String): [Product!]!
}
